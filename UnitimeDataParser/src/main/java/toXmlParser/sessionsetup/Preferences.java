package toXmlParser.sessionsetup;

public class Preferences {
//querySql = "select inimeste_arv,ainekood,nimetus,loeng,praktikum,harjutus from U_COURSEOFFERING";
    // from ClassOptimized if it is too messy contact me
    //int[] makeIntMassiveFromStringInput(String lec, String pra, String har)
    //int[][] makeCourceOfferringData(int[] input)
    //step to create patterns ArrayList<Integer> countDatePattern( int[][] out, int index)
    // String[] getDatePattern( ArrayList<Integer> countdata)
    //String[] getTimePattern(ArrayList<Integer> out)
    //I have not done time/date patterns because there is no access in 193.40.33.53
    //I don no all date patterns so I place some of them in database.get(0)
    // ArrayList<Integer> countdata= [last n weeks, interval in which placed m classes, k-academic lesson(45 min) * 100, m classes]
    // TimePattern String[2]{pattern,pattern} - pattern 1X90(timesAweekXminutes)
}
